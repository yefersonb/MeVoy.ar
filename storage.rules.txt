rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /verificaciones/{uid}/{docKey}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
    // (mantener otras reglas si ya tenías)
  }
}